env:
  contexts:
    - name: example
      urls:
      - https://www.example.com/
  parameters:
    failOnError: true
    failOnWarning: false
    progressToStdout: false
    continueOnFailure: false
    maxDuration: 1

jobs:
  - type: job1
    parameters:

  - type: job2
    parameters:

  - type: job3
    parameters:

  - type: job4
    parameters:
    alwaysRun: true
