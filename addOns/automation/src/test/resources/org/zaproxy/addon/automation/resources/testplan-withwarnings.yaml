env:
  contexts:
    - name: example
      urls: 
      - https://www.example.com/
  parameters:
    failOnError: false                  
    failOnWarning: false               
    progressToStdout: true            

jobs:
  - type: job1
    name: "Job 1"
    parameters:

  - type: job2
    name:
      - First name
      - Second name
    parameters:

  - type: job3
    parameters:
