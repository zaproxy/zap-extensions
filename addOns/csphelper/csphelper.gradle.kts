version = "1"
description = "An add-on for helping to define CSP for a site."

zapAddOn {
    addOnName.set("CSP Helper")
    zapVersion.set("2.7.0")

    manifest {
        author.set("ZAP Dev Team")
    }
}
