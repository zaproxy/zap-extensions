version = "2"
description = "Wavsep report generator."

zapAddOn {
    addOnName.set("Wavsep report generator")
    zapVersion.set("2.9.0")

    manifest {
        author.set("ZAP Dev Team")
        url.set("https://www.zaproxy.org/docs/desktop/addons//")
    }
}
