A copy of the essential files from the zaproxy repo required for running the 
ascan unit tests.

lang/Messages.properties may need to be replaced with a newer version if/when
new messages are referenced as part of initialisation. 